{
  "name": "axios-interface",
  "version": "1.2.3",
  "description": "create interface from axios",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "sideEffects": false,
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "rm -rf lib/ && tsc",
    "lint": "skr lint",
    "test": "yarn build && skr test",
    "prepublishOnly": "yarn run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dancerphil/axios-interface"
  },
  "author": "zhangcong06",
  "license": "UNLICENSED",
  "dependencies": {
    "axios": "^0.20.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "axios-mock-adapter": "^1.17.0",
    "husky": "^1.3.1",
    "reskript": "^0.19.2",
    "typescript": "^3.7.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn lint && yarn test"
    }
  }
}
